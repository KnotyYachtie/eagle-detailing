# Agent instructions (Eagle Detailing)

1. **Always** respect `.cursor/rules/design-assumptions-confidence.mdc` (assumptions vs backbone, confidence, Plain English on new assumption rows).

2. **Before** substantive work on layout, styling, motion, shell, or design-system Markdown: read **`docs/design-system/agent-conductor.md`**, classify the task (A–F), then follow its **read order → code → write order** pipeline.

3. **Prompt stack:** `docs/design-system/design-prompt.md` (brand + tokens + code map) complements the conductor; use both.

Human-first narrative lives under **`documentation/design-system/`**; agent checklists under **`docs/design-system/`**—keep facts aligned (see `agent-conductor.md` §E).
