# Design prompt (human index)

Agent-oriented copy lives in **`docs/design-system/design-prompt.md`** (brand guardrails, tokens, patterns).

**Orchestration (“if this, then that”):** only at **`docs/design-system/agent-conductor.md`** (from repo root: `docs/design-system/` — not under `documentation/`). Read order, write order, handoffs between code, `assumptions.md`, `design.md`, synopsis, and mirrors.

Keep this file to one screen: when the agent prompt or conductor workflow changes, update the paths above—not a second full copy of the prompt here.
